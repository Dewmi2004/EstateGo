{
  "name": "estategoNumbergo-payhere-server",
  "version": "1.0.0",
  "description": "Minimal backend for real PayHere sandbox integration — generates secure hashes and verifies the notify webhook. Run this locally + expose it with ngrok so PayHere's sandbox can reach it.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
